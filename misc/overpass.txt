- Highcharts
- autobahn, crossbar, WAMP
- Aurelia


getSumInReview = (status='InReview') => {
  return this.activeAgents
    .filter(f => f.displayStatus === status)
    .map(m => m[this.StatesMap[status]])
    .reduce((sum, n) => (sum + n, 0));
}

download gps, cdn, olam, chaim, cdn from bitbucket (william@overpass.com/Benjamin001)

- install couchDB
http://127.0.0.1:5984/_utils/#/_all_dbs
create 5 buckets: app, default, state, communication, keyspy

- install crossbar
python, pip, virtualenv


- chaim:
npm install
source params.rc
gulp parse-config
gulp build

- components:
- cdn:

- olam:



///////////////

$ npm install tfk-json-to-xlsx

$ npm install json-2-csv

$ npm install -g istanbul

$  gulp export-test-watch