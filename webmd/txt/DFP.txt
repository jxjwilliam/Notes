slotRenderEnded: This event fires just after the ad's Iframe is constructed.
slot: Returns everything google knows about this ad.
getSlotId: Returns the slot's long identifier from the googletag object.
getDomId: Returns the html ID of this ad's container.
getContentUrl: Returns the URL we want.


I was using EcmaScript5 shimming (prototype library does similar). Remove any such shims, and use something like lodash or underscore.



http://www.javascriptkit.com/javatutors/navigator.shtml


www.qa00.webmd.com/beauty/eyes/ss/slideshow-brows-lashes


http://www.monetizemore.com/blog/stop-using-internet-explorer-for-dfp/


upcate common beacon 



////////////////////////////

1. webmd.adsCookie.js define: ads_perm

webmd.adsCookie.config.perm = {
	name: 'ads_perm', expire: 365 
}

2. webmd.lotame.js:

ads_perm_value = webmd.cookie.getJson('ads_perm',{domain: "webmd.com"});

// drop that value into the ads_perm cookie and make it persistent
webmd.cookie.setJson('ads_perm', ads_perm_value, {domain: "webmd.com", expires:365});


cookieValue = webmd.cookie.getJson('ads_perm', {domain: "webmd.com"}).segvar;


webmd.lotame.truncatedDMPAudiences;
